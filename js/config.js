// Paste your Google Maps API key below to enable the map, trip planner and
// live restaurant info (ratings/photos/hours). See README.md for setup steps.
// Without a key, the app still works: you get the full restaurant list,
// filters, search, visited tracker and "Open in Google Maps" links.
const CONFIG = {
  GOOGLE_MAPS_API_KEY: ""
};
